# BUILT state — what the last build produced from the declared configuration.
# A build succeeding says nothing about whether anything loaded it.
artifact = a17
feature_x = enabled
audit_log = enabled
