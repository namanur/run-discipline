# APPLIED state — the artifact the loader actually put into service.
# This is the system's own record of what it is running, and it is frequently not
# the artifact the runtime reports. When they disagree, the runtime is mistaken
# and the effective layer follows APPLIED.
artifact = a17
